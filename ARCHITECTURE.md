# 架构

## 一句话

电脑连出去、手机连进来，两边挂在同一个配对码下面，中继在中间转发。

## 拓扑

```
              中继 relay（先本地，之后 VPS）
      ┌──────────────────────────────────────┐
      │  /ws?role=host&code=X&token=Y   ← 电脑主动连出来
      │  /ws?role=phone&code=X          ← 手机连进来
      └───────┬──────────────────────┬───────┘
              │                      │
      ┌───────▼────────┐    ┌────────▼─────────┐
      │  电脑           │    │  手机            │
      │  host (Node)   │    │  web (Vue)       │
      │  pi agent      │    │  只是屏幕和输入   │
      │  ★ 你的文件      │    │                  │
      └────────────────┘    └──────────────────┘
```

**为什么需要中继**：电脑在家里/公司网络后面，手机在外面，互相找不到。中继的作用不是
"转发"这么简单 —— 它让**电脑端只需要发起出站连接**，而出站连接不受 NAT 阻挡。
代价是一台公网服务器，收益是用户不用配任何网络。

## 两端不对等 —— 这是 v2 重写的全部原因

v1 的前提是"所有客户端平等，靠 `?session=` 共享一个本地 daemon"。加上中继之后这个
前提塌了：

| | 电脑（host） | 手机（phone） |
|---|---|---|
| 角色 | 服务提供方 | 消费方 |
| 数量 | 1 | 任意多个 |
| 生命周期 | 长期在线，主动重连 | 随时来随时走 |
| 有什么 | 文件、agent、算力 | 一块屏幕 |

v1 那种对称的会话模型套不住这个形状，所以这一层是重写的，不是改的。

## 中继只做一件事

**它不解析消息体。** 判断"这条是谁发的"，转给对面，结束。

```go
if c.role == roleHost {
    c.hub.toControllers(c, msg)
} else {
    c.hub.toHost(c, msg)
}
```

整个 `relay/` 的业务逻辑就是这四行。加一个新功能（审批、文件、未来的任何东西）
只改两端，不用碰 Go。

中继自己只会发出两种消息，都用 `relay:` 前缀和业务消息隔开：

- `relay:presence` —— 电脑上线/掉线了。**没有这条手机只能对着空气打字**，
  而"电脑在不在"恰好只有中继知道。
- `relay:error` —— 配对码不对、被限流。带一个机器可读的 `code` 字段，
  客户端靠它决定怎么办（`no_pairing` 要让手机退回配对界面），而不是去匹配中文。

## 配对

配对码由**电脑端**生成并存在它自己机器上，中继不存任何状态。

三个后果，都是想要的：

1. 中继重启、换机器、扩多实例，已配好的手机都不用重配。
2. 中继不需要数据库。
3. 用户重启电脑，码不变。

**但码本身不是秘密** —— 它是要人从电脑屏幕上念到手机里敲的。所以认的是
`码 + token`，token 只在电脑本地存着。拿对码但 token 不对，中继会拒掉。

带对 token 的重连会**顶掉**旧连接。这是必须的：网络一抖时旧连接多半已经死了但还没被
`readPump` 回收，不顶掉的话这台电脑会一直卡在"码被占用"，而占着它的其实是自己。

## 并发不变量

整套 Go 代码靠一条不变量成立：

> **发送一律在读锁内完成，`remove` 持写锁关 channel，两者天然互斥。**

所以不可能出现"往已关闭的 channel 发送"的 panic。代价是发送必须**非阻塞**
（`select` + `default`），否则一个慢客户端能把整个 Hub 卡住 —— 缓冲写满就踢掉它。

这条不是靠注释自证的：`relay/hub_test.go` 里四个广播者和四个"连上就走"的手机
把它逼到真正重叠，`pnpm test` 跑。**验过它有牙**——把发送挪到读锁外面
（那个看起来最自然的重构），0.8 秒就 panic。

## 中继上的两个上界

任何人都能拿随机码连过来说自己是 host，中继会为它建一条配对。所以有两道闸：

- **限流**（`pair.go`）：按来源 IP 记账。手机猜错码、拿错 token 冒充电脑、
  新建配对，都算一次失败，连续 10 次关 15 分钟小黑屋。
- **回收**（`hub.go` 的 `reapLoop`）：电脑离线超过 `-pair-idle`（默认 10 分钟）
  且没人在看的配对会被收掉。

限流压住灌入的速度，回收给出内存的上界，两个一起才关得住。

---

## 踩过的坑

这些都是实际写出来才发现的，不是从文档里抄的。重写时不该踩第二遍。

### Go / WebSocket

1. **`scanner.Bytes()` 复用同一块底层数组** —— 交出去之前必须复制。
2. **`append(line, '\n')` 会就地改写调用方的切片** —— 要复制进新 frame。
3. **`bufio.Scanner` 默认只给 64KB** —— 大内容会溢出，要 `scanner.Buffer`。
4. **收尾顺序：先广播再断开** —— `c.send` 是带缓冲的，关掉后 `writePump`
   仍会把缓冲写完才退出。
5. **`remove` 必须幂等** —— 双重 `close(c.send)` 直接 panic。
6. **`hub.add` 要发生在 `writePump` 起来之前** —— 否则这期间的消息全丢。
7. **回收器的 tick 要用 `idle/4`** —— 用 `idle` 的话最坏要等 `2×idle`。
8. **从锁里往外调函数要当心** —— `notifyPresence` 要读锁，所以它不能在
   `removeLocked` 的写锁里调。`detach` 这个包装就是为这个存在的。

### Node / 浏览器

9. **Node 的 WebSocket 连接失败只触发 `error`、不触发 `close`**（undici 的行为）。
   只监听 `onclose` 的话，中继没起来时进程会一声不响地以退出码 0 结束 ——
   表现得像"启动成功了但没反应"。**浏览器不是这样**，那边 `onclose` 会触发。
10. **`cancel` 必须绕开 prompt 队列** —— 它要打断的正是队列头部那个正在跑的，
    排到队尾等于永远不生效。
11. **路径沙箱用 `relative()`，不要用字符串前缀比较** —— 前者自己处理 `./` `../`
    和 Windows 盘符大小写。（M2 会用到）
12. **同一个配对码只能有一个 host 进程，而且撞车时是静默的。**
    两个进程读的是同一份 `~/.goagent/config.json`，所以码和 token 都一样。
    于是 A 连上 → 顶掉 B → B 一秒后重连 → 顶掉 A →……**永远打下去**。
    真正难查的地方在于：中继日志只是每秒一行「电脑已接入」，看起来一切正常；
    而手机发去的 prompt 会被**正在被顶掉的那个 host** 接走，模型跑完，
    结果发给一条已经关掉的连接 —— `send()` 见 `readyState` 不是 OPEN 就静默丢弃，
    表现成「电脑没反应」。
    两个修法缺一不可：中继顶人之前先塞一条 `relay:error{code:'evicted'}` 进缓冲
    （`Hub.evict`，必须持写锁、必须在 `close` 之前），host 收到就退出而不是重连；
    以及 **退避不能在 `onopen` 里立即清零** —— 那样「连上就死」的循环永远停在 1 秒。

### 环境的

13. **`go run` 在 Windows 上会锁住源码目录** —— 进程不退，`rm -rf` 就报
    `Device or resource busy`。
14. **`vue-tsc` 还不认 TypeScript 7** —— `typescript` 要停在 5.x，否则
    `typecheck` 会以 `ERR_PACKAGE_PATH_NOT_EXPORTED` 挂掉。
15. **配对码的字母表里没有 `0 1 I L O`** —— 测试里造的码也必须守这个规矩，
    否则会被中继在校验那步就以 `bad_request` 拒掉，根本走不到想测的分支。
