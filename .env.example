# 复制成 .env，填自己的 key。.env 已被 .gitignore 拦住，不会入库。
DEEPSEEK_API_KEY=

# 下面这些都有默认值，不填也能跑，列在这儿是为了让人知道它们存在。

# 中继地址。挪到 VPS 之后改成 ws://你的域名:8080
# GOAGENT_RELAY=ws://localhost:8080

# GOAGENT_MODEL=deepseek-v4-flash

# 配对码和 token 存哪。默认 ~/.goagent/config.json
# GOAGENT_CONFIG=
